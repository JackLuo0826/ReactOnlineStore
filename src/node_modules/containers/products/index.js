import React, { Component } from 'react';
import { connect } from 'react-redux';
import { Link } from 'react-router';
import R from 'ramda';

import { fetchProducts, loadMoreProducts, addProductToBasket, fetchCategories } from 'actions';
import { getProducts } from 'selectors';

class Products extends Component {
    componentDidMount() {
        this.props.fetchProducts();
        this.props.fetchCategories();
    }

    renderProduct (product, index) {
        const { addProductToBasket } = this.props;
        const shortDescription = `${R.take(60, product.description)}...`;

        return(
            <div className="col-sm-4 col-lg-4 col-md-4 book-list" key={index}>
                <div className="thumbnail">
                    <img
                        className="img-thumbnail"
                        src={product.image}
                        alt={product.name}
                    />
                </div>
                <div className="caption">
                    <h4 className="pull-right">${product.price}</h4>
                    <h4>
                        <Link to={`/products/${product.id}`}>
                            {product.name}
                        </Link>
                    </h4>
                    <p>{shortDescription}</p>
                    <p className="itemButton">
                        <button onClick={() => addProductToBasket(product.id)} className="btn btn-primary">
                            Buy Now!
                        </button>
                        <Link to={`/products/${product.id}`}>
                            <button className="btn">More info</button>
                        </Link>
                    </p>
                </div>
            </div>
        );
    }

    render() {
        const {products, loadMoreProducts} = this.props;
        //console.log('products', products);
        return (
            <div>
                <div className="books row">
                    {products.map((product, index) => this.renderProduct(product, index))}
                </div>
                <div className="row">
                    <div className="col-md-12">
                        <button onClick={loadMoreProducts} className="pull-right btn btn-primary">
                            Load More
                        </button>
                    </div>
                </div>
            </div>
        );
    }
}

const mapStateToProps = (state, ownProps) => ({
    products: getProducts(state, ownProps)
});

const mapDispatchToProps = {
    fetchProducts,
    loadMoreProducts,
    addProductToBasket,
    fetchCategories
};

export default connect(mapStateToProps, mapDispatchToProps)(Products);